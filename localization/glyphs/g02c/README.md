# G02C — Human-assisted runtime certification

This workspace is a validation record for the G02 dynamic Xbox LT glyph pilot. It contains no source changes, localization changes, runtime executable changes, save changes, or fabricated screenshots.

Authoritative pilot: message `0x100D`, the existing Kokiri tutorial prompt containing `[Z]`. Target: LG Ultrawide at 3440×1440, 85 Hz. Required controller: Xbox Series X Controller.

Because the native window cannot be reliably observed by the automation environment, visual and physical claims require explicit human confirmation and manually captured screenshots.
