# Visual comparison

Pending human evidence. This document must not claim Classic or Dynamic visual PASS until both manually captured screenshots exist and the user explicitly confirms the observations.

Expected comparison at the same `0x100D` prompt and 3440×1440 target:

| Profile | Expected inline glyph |
|---|---|
| CLASSIC | Original N64 Z |
| DYNAMIC + Xbox LT | Xbox LT |

Check clipping, stretching, overlap with text, orientation, size, readability at normal couch distance, flicker, and message-box integrity. Desktop/window chrome dimensions must be recorded if the image is not exactly 3440×1440.
